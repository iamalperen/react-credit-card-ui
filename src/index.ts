/**
 * Main entry point for the Cardify credit card component library
 * Exports the CreditCard component for use in applications
 */
import CreditCard from './components/CreditCard';

export { CreditCard };
